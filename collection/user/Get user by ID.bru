meta {
  name: Get user by ID
  type: http
  seq: 1
}

get {
  url: {{endpoint}}/user/id/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Usuário por ID
  
  Retorna dados do usuário pelo ID.
}
