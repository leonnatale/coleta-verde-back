meta {
  name: List all
  type: http
  seq: 2
}

get {
  url: {{endpoint}}/address/all
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Listar endereços
  
  Lista todos os endereços do usuário logado.
}
