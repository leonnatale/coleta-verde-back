meta {
  name: create
  seq: 1
}

docs {
  # Criação de uma solicitação
  
  # Body data
  ```
  {
      authorId: number,
      type: 'rubble' | 'recycle',
      addressIndex: number,
      desiredDate: string,
      description: string,
      suggestedValue: number
  }
  ```
}
